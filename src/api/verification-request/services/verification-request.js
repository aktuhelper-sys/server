'use strict';

/**
 * verification-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::verification-request.verification-request');
