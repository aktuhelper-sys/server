'use strict';

/**
 * verification-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::verification-request.verification-request');
